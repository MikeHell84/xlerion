# === CONFIGURACIÓN DE TOTAL DARKNESS ===
# Este es un archivo de EJEMPLO. Copia este contenido a .env.local
# NUNCA commits .env con valores reales a Git

# ============================================
# CONFIGURACIÓN SMTP (Servidor de Correos)
# ============================================
# Estos valores deben configurarse con las credenciales reales del servidor de correos
# Contacta al administrador del servidor para obtener los valores correctos

# Servidor SMTP (ej: mail.xlerion.com, smtp.gmail.com, etc.)
SMTP_HOST=mail.xlerion.com

# Puerto del servidor SMTP (típicamente 465 para SSL, 587 para TLS, 25 para no encriptado)
SMTP_PORT=465

# Usuario de autenticación SMTP (típicamente el email)
SMTP_USERNAME=support@xlerion.com

# Contraseña de autenticación SMTP
# ¡¡¡ IMPORTANTE: Nunca commits esta contraseña con el valor real !!!
SMTP_PASSWORD=TU_CONTRASEÑA_SMTP_AQUI

# ============================================
# CONFIGURACIÓN EMAIL
# ============================================
# Email y contraseña para sistema de recuperación (puede ser igual a SMTP)
EMAIL_USER=contactus@xlerion.com
EMAIL_PASSWORD=TU_CONTRASEÑA_EMAIL_AQUI

# ============================================
# CONFIGURACIÓN DEL ENTORNO
# ============================================
# Valores posibles: development, production
# En desarrollo: se pueden mostrar mensajes de error detallados
# En producción: se ocultan detalles técnicos
ENVIRONMENT=production

# ============================================
# CONFIGURACIÓN DE BASE DE DATOS
# ============================================
# Ubicación del archivo SQLite (relativo a la raíz del proyecto)
DB_PATH=./data/admins.db

# ============================================
# CORS Y SEGURIDAD
# ============================================
# Dominio permitido para CORS en producción
# En desarrollo: localhost:5173
# En producción: https://xlerion.com
ALLOWED_ORIGIN=https://xlerion.com

# ============================================
# INSTRUCCIONES DE USO
# ============================================
# 1. Copia este archivo a: .env.local
#    > cp .env.example .env.local
#
# 2. Edita .env.local con tus valores reales:
#    - SMTP_PASSWORD: contraseña del servidor de correos
#    - EMAIL_PASSWORD: contraseña del email
#    - ALLOWED_ORIGIN: tu dominio de producción
#
# 3. Asegúrate de que .env.local esté en .gitignore (NO debe subirse a Git)
#
# 4. El código cargará automáticamente los valores desde .env.local
#
# 5. Nunca commits credenciales reales en Git
#
# Para más información sobre variables de entorno en PHP:
# https://www.php.net/manual/es/function.getenv.php
